# Builder learning-to-builder bridge gate template.
# Copy to `.dev/builder_learning_bridge.env` before running the gate.

# 0 = no learning-triggered patch this cycle (gate passes as not_triggered).
# 1 = learning-triggered patch is requested (all fields below become mandatory).
LEARNING_TRIGGERED=0

# Must be 1 before Builder can consume the report.
LEARNING_REPORT_VALIDATED=0

# Report metadata.
LEARNING_REPORT_ID=
LEARNING_REPORT_FILE=.dev/builder_learning_report.md
LEARNING_SOURCE_ENGINES=PH1.FEEDBACK,PH1.LEARN,PH1.KNOW
LEARNING_SIGNAL_COUNT=0
