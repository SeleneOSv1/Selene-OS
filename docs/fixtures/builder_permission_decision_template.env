# Builder decision ingest template (for scripts/apply_builder_permission_decision.sh).
# Copy and fill one file per decision event (code/launch).

# Required
PHASE=code
DECISION=approve

# Required for approve/deny
BCAST_ID=
DECISION_REF=

# Optional for pending busy flow
REMINDER_REF=

# Optional overrides
BUSY_MODE_OVERRIDE=
REFRESH_DAILY_REVIEW=1
