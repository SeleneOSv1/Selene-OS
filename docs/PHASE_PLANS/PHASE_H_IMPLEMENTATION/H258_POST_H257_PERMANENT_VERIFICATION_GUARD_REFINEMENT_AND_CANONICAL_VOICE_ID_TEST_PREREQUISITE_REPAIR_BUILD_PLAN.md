# H258 Post-H257 Permanent Verification-Guard Refinement and Canonical Voice-ID Test-Prerequisite Repair Build Plan

## Objective

H258 is the first permanent verification-guard refinement and canonical voice-ID test-prerequisite repair build after H257.

H258 does not reverse H257, H256, H255, H254, H253, H252, H251, H250, H249, H248, H247, H246, H245, H244, H243, H237, H112, H104, or H236 truth.

H258 preserves the live APP_MAC_DESKTOP frontier.

No new APP_MAC_DESKTOP executable unit is selected in this run.

No new next exact winner is published in this run.

## Basis

H257 remains live as the first canonical post-H256 selected bounded native macOS `desktop_invite_open_and_onboarding_entry` implementation build.

`AGENTS.md` remains the active auto-loaded execution law for this repository.

A follow-on H257 verification review exposed two inherited repair needs:

- the self-authored forbidden-symbol verification clause was too broad because it scanned whole target files even when same-task repo truth already preserved older symbols such as `remaining_platform_receipt_kinds` outside the H257 diff, which made the clause unable to distinguish inherited truth from current-task behavior
- the exact shared adapter HTTP invite-onboarding-and-voice e2e prerequisite was blocked by a canonical contract mismatch between shared PH1.VOICE.ID engine unknown-score carriage and app-ingress canonical fail-closed validation, not by H257 product-slice behavior

Same-task repo truth showed the exact shared conflict:

- [ph1_voice_id.rs#L716](/Users/selene/Documents/Selene-OS/crates/selene_engines/src/ph1_voice_id.rs#L716) emitted reason-specific unknown `score_bp` values such as `0`, `3000`, `3500`, `6000`, and `6200`
- [app_ingress.rs#L5182](/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs#L5182) and [app_ingress.rs#L5663](/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs#L5663) required canonical posture and recovery fail-closed `score_bp` carriage of `2000` or `4500`
- [runtime_governance.rs#L2697](/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs#L2697) already preserved the same canonical `SpeakerAssertionUnknown::v1_with_candidate(...)` carrier family used by app-ingress truth rather than the legacy engine-specific score mapping

Under existing repo-truth precedence, the canonical forwarded-voice and app-ingress fail-closed contract remains the controlling authority for downstream execution truth.

Therefore H258 must:

- permanently refine repository agent law so future verification clauses use diff-scoped or slice-scoped checks when preserved symbols already exist outside the current task slice
- permanently require exact same-task test-name listing plus nonzero executed-test verification before an exact test command can count as proof
- repair the shared PH1.VOICE.ID engine unknown-score mapping so it matches the canonical app-ingress fail-closed contract already preserved elsewhere in repo truth

## Build Result

H258 permanently refines repository agent law by requiring:

- diff-scoped or equivalently narrow verification when a forbidden-symbol clause is intended to prove that the current task did not add a forbidden carrier and same-task repo truth already shows preserved conflicting symbols outside the current diff
- exact same-task runnable-name verification for cited exact tests when the runner path could be ambiguous
- explicit nonzero executed-test verification for cited exact tests; a `0 tests` result is not proof
- inherited-prerequisite diagnosis before blaming the current target slice for an exact-test failure

H258 also repairs the shared canonical test prerequisite by aligning PH1.VOICE.ID unknown fail-closed `score_bp` carriage in [ph1_voice_id.rs#L716](/Users/selene/Documents/Selene-OS/crates/selene_engines/src/ph1_voice_id.rs#L716) with the canonical posture and recovery fail-closed contract already preserved in [app_ingress.rs#L5182](/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs#L5182) and [app_ingress.rs#L5663](/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs#L5663).

The exact repaired verification prerequisite proof in this run is:

- `cargo test -p selene_adapter --lib tests::run1_invite_click_adapter_starts_onboarding_without_turn_or_client_time_inputs -- --exact`
- `cargo test -p selene_adapter --bin selene_adapter_http tests::ingress_iphone_invite_onboarding_and_explicit_voice_turn_e2e -- --exact --nocapture`

The exact repaired canonical score-regression proof in this run is:

- `cargo test -p selene_engines --lib ph1_voice_id::tests::at_vid_15b_fail_closed_unknown_scores_match_canonical_runtime_contract -- --exact`

## Publication Result

H258 is now published as the permanent verification-guard refinement and canonical voice-ID test-prerequisite repair build.

`AGENTS.md` now requires diff-scoped or slice-scoped forbidden-symbol verification when preserved symbols already exist outside the current task diff, plus exact runnable-name listing and nonzero executed-test verification for cited exact tests.

Shared PH1.VOICE.ID engine unknown fail-closed `score_bp` carriage now aligns with the canonical app-ingress and runtime-governance fail-closed contract.

H257 truth remains preserved.

H256 truth remains preserved.

H255 truth remains preserved.

H254 truth remains preserved.

H253 truth remains preserved.

H252 truth remains preserved.

H251 truth remains preserved.

H250 truth remains preserved.

H249 truth remains preserved.

H248 truth remains preserved.

The umbrella desktop program remains preserved.

The master completion plan and master completion ledger remain preserved as authoritative live-truth carriers.

No wake parity claim is made.

No proven native macOS wake-listener integration claim is made.

No autonomous unlock claim is made.

No post-H258 next exact winner is published in this run.

## Out Of Scope

H258 does not authorize:

- any new APP_MAC_DESKTOP frontier selection
- any broad whole-file negative verification clause that cannot distinguish preserved repo truth from the current diff when that distinction is discoverable
- any claim that an exact test proved a result when the runner executed zero tests
- any new onboarding-continue, wake-listener, or autonomous-unlock implementation slice
