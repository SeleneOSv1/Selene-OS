# H187 Section07 Closure Program A Through E Implementation-First Build Plan

## Objective

H187 records the implementation-first closure program for the remaining Section 07 residual work:

- `S07-05`
- `S07-06`
- `S07-07`
- `S07-08`
- `S07-09`
- `S07-10`
- `S07-11`
- `S07-13`
- `S07-17`
- `S07-19`
- `S07-18`

H187 exists to give the repository one explicit execution program for true Section 07 closure.

H187 is not a docs-first run.

H187 is not a claim that one last tiny helper patch will finish Section 07.

H187 is not required in order to begin Section 08 work.

H187 is only the correct path if the goal is to actually drain the remaining Section 07 residual rows.

## Basis

Current master-doc authority still preserves the following Section 07 rows as unresolved:

- `S07-05`
- `S07-06`
- `S07-07`
- `S07-08`
- `S07-09`
- `S07-10`
- `S07-11`
- `S07-13`
- `S07-17`
- `S07-19`

and preserves:

- `S07-18` as `NOT_EXPLICIT`

Current repo truth also preserves all of the narrower governed forwarded-voice and proof-carrier
progress already landed through H181, H182, H184, H185, and H186.

Those narrower builds materially narrowed local helper seams, but they did not eliminate the broader
blocker wording carried in the remaining Section 07 rows.

The preserved row blockers show that the remaining work is broader closure work, not one more
automatic micro-build:

- broader runtime adoption remains partial
- broader anti-spoof / liveness / verification closure remains partial
- broader step-up / recovery closure remains partial
- generalized cross-node closure remains partial
- explicit identity certification targets are still not exposed by current repo truth

Earlier H19 planning truth correctly preserved the next exact winner as `NOT_EXPLICIT` because repo
truth did not expose one unique smallest safe implementation seam.

H187 does not deny that H19 was correct at the time.

H187 instead records the later decision to replace the single-winner expectation with an explicit
multi-program implementation-first closure program.

## Program Decision

The remaining Section 07 work is grouped into five implementation programs:

- Program A: runtime adoption closure
- Program B: spoof / posture / verification closure
- Program C: step-up / recovery closure
- Program D: cross-node and drift closure
- Program E: explicit certification target surfacing

Each program may contain multiple later implementation builds.

Each later implementation build must still stop if current repo truth does not expose a lawful
code-bearing seam.

Under the active repository build law, every later `build` under H187 must still mean
implementation.

## Program A

### Rows

- `S07-05`
- `S07-08`
- `S07-19`

### Goal

Close broader runtime adoption of canonical identity and voice carrier transport, access fail-closed
identity-context surfacing, and missing-simulation identity-context surfacing.

### Why These Rows Belong Together

These rows all remain open because current repo truth still carries only the first artifact-governed
caller-adoption slice plus a narrow governed forwarded-voice proof envelope.

The unresolved work is broader caller adoption and broader canonical runtime attachment, not more
local wording or one more helper-family refinement.

### Expected Implementation Surfaces

- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_law.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1_voice_id.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1j.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_kernel_contracts/src/runtime_execution.rs`

### Done When

- canonical identity and voice carrier adoption is no longer limited to the first governed caller
  slice
- access fail-closed identity-context carriage is generalized across the lawful downstream runtime
  surfaces
- missing-simulation PH1.X identity-context surfacing is generalized across the lawful downstream
  runtime surfaces
- the row blockers no longer truthfully require "broader runtime adoption remains partial"

### Stop Conditions

Stop Program A sub-build selection if:

- the exposed seam is docs-only
- the exposed seam is still purely local to governed forwarded-voice and does not broaden runtime
  adoption
- the remaining work still collapses into broader Section 11 carrier adoption without an executable
  code-bearing slice

## Program B

### Rows

- `S07-06`
- `S07-07`
- `S07-09`

### Goal

Close broader spoof, liveness, posture, confidence, and verification adoption.

### Why These Rows Belong Together

These rows already have narrow governed forwarded-voice posture mapping and fail-closed surfacing.

They remain open because broader posture-family adoption is still partial outside that narrow
surface.

### Expected Implementation Surfaces

- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_law.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1_voice_id.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1j.rs`

### Required Closure Families

Program B must ultimately close broader runtime handling for:

- `VID_SPOOF_RISK`
- `VID_FAIL_LOW_CONFIDENCE`
- gray-zone margin posture
- `VID_FAIL_ECHO_UNSAFE`
- `VID_FAIL_NO_SPEECH`
- `VID_FAIL_MULTI_SPEAKER_PRESENT`

### Done When

- these posture families are carried through canonical runtime surfaces instead of staying as narrow
  governed forwarded-voice-only truth
- anti-spoof, liveness, confidence, and verification closure no longer truthfully remain partial

### Preserve

- governance-quarantine must not be silently widened here
- hard-deny contrasts must not be reclassified without new code basis

### Stop Conditions

Stop Program B sub-build selection if:

- the next seam only narrows wording and does not broaden posture adoption
- the seam still depends on overturning a preserved contrast with no new runtime basis

## Program C

### Rows

- `S07-10`
- `S07-13`

### Goal

Close broader step-up, recovery, reauth, re-enrollment, device-claim, and related recovery posture
adoption.

### Why These Rows Belong Together

These rows already contain local helper alignments for:

- `VID_REAUTH_REQUIRED`
- `VID_DEVICE_CLAIM_REQUIRED`

and preserve nearby contrasts, but broader runtime-side step-up and recovery adoption still remains
partial.

### Expected Implementation Surfaces

- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/app_ingress.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_law.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1_voice_id.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1j.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_kernel_contracts/src/runtime_execution.rs`

### Required Closure Families

Program C must ultimately close broader canonical handling for:

- device-claim
- reauth
- re-enrollment
- profile-not-enrolled
- recovery-restricted postures where lawfully supported

### Done When

- broader step-up and recovery semantics are canonical across the lawful runtime carrier surfaces
- broader step-up / recovery / reenrollment closure no longer truthfully remains partial

### Preserve

- governance-quarantine remains denied / not-requested unless fresh repo truth explicitly exposes a
  lawful semantic change

### Stop Conditions

Stop Program C sub-build selection if:

- the next seam depends on overturning preserved governance-quarantine truth without a new carrier
  basis
- the only visible work is another local helper alignment with no broader runtime adoption effect

## Program D

### Rows

- `S07-11`
- `S07-17`

### Goal

Close generalized cross-node identity consistency and drift-monitoring closure.

### Why These Rows Belong Together

Cross-node consistency and drift propagation are currently preserved as broader residuals because
repo truth still shows local posture surfacing and local drift visibility, not generalized
cross-node runtime and storage closure.

### Expected Implementation Surfaces

- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_law.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_storage/src/repo.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1j.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_kernel_contracts/src/runtime_execution.rs`

### Done When

- cluster and policy drift propagation is canonicalized
- cross-node identity state becomes comparable, enforceable, and provable through runtime and
  storage carriers
- cross-node identity consistency no longer truthfully remains a broader residual
- drift monitoring no longer truthfully remains a broader residual

### Stop Conditions

Stop Program D sub-build selection if:

- the candidate seam only adds more local drift messages
- storage and runtime carriers still do not expose a lawful generalized cross-node implementation
  path

## Program E

### Row

- `S07-18`

### Goal

Expose and land explicit identity certification targets.

### Why This Program Is Separate

`S07-18` does not merely remain partial. It is still `NOT_EXPLICIT`.

That means the missing work is not just broader adoption. The repo still needs an explicit target
representation in live runtime, proof, and storage surfaces.

### Expected Implementation Surfaces

- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1j.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_governance.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_os/src/runtime_law.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_kernel_contracts/src/ph1j.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_kernel_contracts/src/runtime_execution.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs`
- `/Users/selene/Documents/Selene-OS/crates/selene_storage/src/repo.rs`

### Done When

- current repo truth explicitly exposes identity certification targets
- those targets are carried through runtime, proof, and storage surfaces
- at least one end-to-end target-aware path is live and test-proven
- `S07-18` no longer truthfully remains `NOT_EXPLICIT`

### Stop Conditions

Stop Program E sub-build selection if:

- the only proposal is to describe targets in docs
- the target model would exceed current proof/reference carrier limits without deliberate contract
  and storage work
- no executable target-bearing runtime surface is exposed yet

## Execution Order

Default execution order is:

1. Program A
2. Program B
3. Program C
4. Program D
5. Program E

This order is preferred because:

- Program A broadens the runtime carrier surfaces that Programs B and C still depend on
- Programs B and C broaden posture and recovery adoption before cross-node and target closure
- Program D depends on broader runtime and storage carriage than the currently narrowed local
  governed forwarded-voice seams
- Program E should come last because explicit certification targets need stable runtime, proof, and
  storage carriers

A later build may run out of order only if current repo truth exposes an independent lawful
implementation seam that does not rely on unresolved carriers from an earlier program.

## Child Build Naming

Each executable implementation slice under H187 should be named as a child build:

- `H187A1`, `H187A2`, ...
- `H187B1`, `H187B2`, ...
- `H187C1`, `H187C2`, ...
- `H187D1`, `H187D2`, ...
- `H187E1`, `H187E2`, ...

Each child build must:

- name the exact behavior to change
- name the exact files expected to change
- name the exact tests that prove the change
- stop if current repo truth does not expose a lawful code-bearing seam

## Repository Law For This Program

Under the active repository execution law:

- every later H187 child build must treat `build` as `implementation`
- docs-only work is not a lawful automatic next build
- master-doc drift is never enough to select the next build
- if no safe implementation seam is exposed for a candidate child build, that child build must
  report:
  - `no lawful implementation build exposed by current repo truth`

## Docs Policy

Default:

- no doc change

Allowed only after a landed implementation slice:

- minimal tail updates that truthfully narrow the corresponding Section 07 row wording

Not allowed:

- docs-first execution
- docs-only closure claims
- row completion claims without code and exact tests

## Completion Standard

Section 07 is not complete until all of the following are true:

- Program A is complete
- Program B is complete
- Program C is complete
- Program D is complete
- Program E is complete
- the remaining Section 07 row blockers no longer truthfully require `PARTIAL`
- `S07-18` no longer truthfully requires `NOT_EXPLICIT`

Until then, Section 07 remains open even if individual H187 child builds land.
