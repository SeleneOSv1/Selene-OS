# H248 Post-H247 Permanent Lean Obvious-Slice Direct-Implementation Law Change Build Plan

## Objective

H248 is the first permanent lean obvious-slice direct-implementation law change after H247.

H248 does not reverse H247, H246, H245, H244, H243, H237, H112, or H104 truth.

The umbrella desktop program remains preserved.

The master completion plan and master completion ledger remain authoritative live-truth carriers.

This run changes next-build agent law only.

No executable unit is selected in this run.

No implementation occurs in this run.

## Basis

The umbrella desktop program remains live at [APP_MAC_DESKTOP_REAL_OPERATION_PROGRAM.md#L1](/Users/selene/Documents/Selene-OS/docs/PHASE_PLANS/PHASE_H_IMPLEMENTATION/APP_MAC_DESKTOP_REAL_OPERATION_PROGRAM.md#L1).

H243 remains live as the first canonical post-H236 intentional broader APP_MAC_DESKTOP real-operation foundation selection.

H245 remains live as the first canonical post-H244 selected bounded native macOS `desktop_permission_posture` implementation build.

H247 remains live as the first canonical post-H246 selected bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate` implementation build.

H237 remains live as the permanent same-frontier docs-only publication loop freeze selection-law change.

H112 remains live as executable-unit anti-loop authority.

H104 intentional-selection authority remains preserved.

Current live desktop repo truth is already being carried by:

- the umbrella desktop program
- the master completion plan
- the master completion ledger

The recurring remaining failure mode is now explicit: after the umbrella program exists and the master docs already preserve current frontier truth, repeated standalone docs-only exact-selection publication can still become procedural overhead even when the next exact executable unit is already obvious from live repo truth.

That heavier publication-first habit is no longer necessary on every obvious next slice.

## Lean Obvious-Slice Direct-Implementation Agent Law

H248 permanently refines H104, H112, and H237 by adding a lean obvious-slice direct-implementation agent law.

The umbrella desktop program remains required for the full-system target.

The master completion plan and master completion ledger remain required live-truth carriers for the current frontier state.

A separate docs-only exact executable-unit selection publication remains lawful and required only when the next slice is genuinely ambiguous or risky.

Genuinely ambiguous or risky means one or more of:

- live repo truth exposes multiple plausible exact executable seams
- the next exact seam is not clearly the smallest coherent implementation unit
- broader coupling, hidden consequences, or authority-law risk make the seam non-obvious
- direct implementation would likely widen beyond the currently preserved frontier
- preserved law / governance / authority posture requires an explicit standalone selection record before editing source

If the umbrella program and master docs already preserve the current frontier, and the next exact executable unit is obvious from live repo truth, the lawful next move is direct implementation build.

That direct implementation build must still:

- state the exact selected executable unit inside the implementation build itself
- preserve the umbrella program
- preserve master-doc truth catch-up
- preserve all applicable non-claims and bounded posture

If mandatory reads or start checks reveal genuine ambiguity or risk after a direct implementation build was proposed, the build must stop and publish a standalone selection build instead.

H248 does not eliminate selection publication.

H248 narrows selection publication to the cases where it is actually needed.

## Publication Result

H248 is now published as the permanent lean obvious-slice direct-implementation law change.

The umbrella desktop program remains preserved.

The master completion plan and master completion ledger remain preserved as the authoritative live-truth carriers.

Separate docs-only exact-selection publication is no longer mandatory on every next slice.

Separate docs-only exact-selection publication remains required when the next slice is genuinely ambiguous or risky.

When the next exact slice is obvious from live repo truth and master-doc truth already preserves the frontier, the lawful next move is direct implementation build.

H237 same-frontier loop-freeze truth remains preserved.

H112 executable-unit selection truth remains preserved.

H104 intentional-selection truth remains preserved.

No post-H248 next exact winner is published in this run.

## Out Of Scope

H248 does not authorize:

- removal of the umbrella desktop program
- removal of the master completion plan
- removal of the master completion ledger
- skipping master-doc truth catch-up
- skipping start checks or mandatory reads
- any source-code edit in this run
- any runtime-law, governance, or authority reversal
- any claim that H237 was false
- any claim that H112 was false
- any claim that H104 was false
- any claim of wake parity
- any claim of proven native macOS wake-listener integration
- any claim of autonomous unlock
- any next executable-unit publication in this run
