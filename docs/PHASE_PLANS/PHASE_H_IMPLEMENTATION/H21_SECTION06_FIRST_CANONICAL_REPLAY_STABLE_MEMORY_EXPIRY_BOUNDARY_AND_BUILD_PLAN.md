# H21 Section 06: First Canonical Replay-Stable Memory Expiry Boundary and Build Plan

## Objective
Current published repo truth is that Section 06 remains the next partial memory-closure area after the published Section 05 H20 slice, and the replay-stable expiry gap remains a real Section 06 blocker. This document still freezes that replay-stable PH1.M memory expiry gap from current repo evidence, but it now corrects the implementation claim after the failed H22 attempt proved that the previously assumed same-file replay carrier was overstated. The lawful current repo-truth outcome is therefore that the gap is real, but the next Section 06 implementation winner is not yet explicit.

## Current Published Repo Truth
Section 06 remains live but partial by current repo truth at [MASTER_BUILD_COMPLETION_PLAN.md#L89](/Users/selene/Documents/Selene-OS/docs/MASTER_BUILD_COMPLETION_PLAN.md#L89), [COVERAGE_MATRIX.md#L10](/Users/selene/Documents/Selene-OS/docs/COVERAGE_MATRIX.md#L10), and [MASTER_BUILD_COMPLETION_LEDGER.md#L225](/Users/selene/Documents/Selene-OS/docs/MASTER_BUILD_COMPLETION_LEDGER.md#L225). The architecture still describes broader PH1.M retention and lifecycle closure as incomplete at [CORE_ARCHITECTURE.md#L4437](/Users/selene/Documents/Selene-OS/docs/CORE_ARCHITECTURE.md#L4437), so this H21 boundary is not a claim that all of Section 06 is finished.

Current published repo truth already exposes the exact replay-stable expiry seam. PH1.M derives expiry before persistence in `derive_expires_at_from_ledger_event` at [ph1m.rs#L294](/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1m.rs#L294) and threads that derived `expires_at` into `ph1m_append_ledger_row` inside `persist_memory_forwarded_outcome` at [ph1m.rs#L338](/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1m.rs#L338). PH1.F already preserves that expiry when applying current-view state through `apply_memory_event_to_current` at [ph1f.rs#L3919](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L3919). But `rebuild_memory_current_from_ledger()` currently replays with `apply_memory_event_to_current(..., None)` at [ph1f.rs#L3972](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L3972), which drops replay-time expiry semantics even though the append path already preserved them.

Current published repo truth also already exposes the bounded proof surface for this gap in the same file. The existing rebuild proof family compares `memory_current()` before and after rebuild at [ph1f.rs#L24170](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L24170). But the failed H22 attempt also proved a stricter carrier mismatch inside the same file: `MemoryLedgerRow` at [ph1f.rs#L1022](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L1022) does not expose `expires_at`, while `expires_at` is present only on `MemoryCurrentRecord` at [ph1f.rs#L1030](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L1030). Current repo truth therefore still exposes the replay gap, but it does not yet expose one lawful same-file replay carrier for fixing it without further carrier-selection work.

## Exact Winner
The first canonical Section 06 gap remains replay-stable PH1.M memory expiry preservation during `memory_current` rebuild. The producer still derives expiry upstream, the append path still preserves it, and the rebuild path still demonstrably drops it. But current repo truth no longer supports the stronger claim that the next implementation winner is already explicit and lawfully bounded to one same-file `ph1f.rs` fix.

The corrected current truth is that this replay gap remains the active Section 06 planning blocker, while the next implementation winner is `NOT_EXPLICIT`. Publishing any later slice would still be narrower than generalized PH1.M lifecycle closure, but current repo truth must first expose one lawful carrier for replay-time expiry recovery before implementation can be frozen again.

## Primary Carrier And Boundary
The primary producer boundary still begins in PH1.M when `persist_memory_forwarded_outcome` derives one deterministic `expires_at` value from one ledger event at [ph1m.rs#L314](/Users/selene/Documents/Selene-OS/crates/selene_os/src/ph1m.rs#L314). The primary consumer boundary still ends in PH1.F when `rebuild_memory_current_from_ledger()` reconstructs current state from ledger rows at [ph1f.rs#L3972](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L3972).

But current repo truth now shows that the replay carrier inside PH1.F is not explicit enough to freeze one lawful implementation slice. `apply_memory_event_to_current` remains the canonical current-view mutation edge at [ph1f.rs#L3919](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L3919), yet the replay rows feeding that edge are `MemoryLedgerRow` values that do not store `expires_at` at [ph1f.rs#L1022](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L1022). Current repo truth therefore does not yet expose one lawful implementation carrier inside the previously claimed same-file boundary, and the next Section 06 implementation winner is `NOT_EXPLICIT` until a carrier-selection audit resolves the replay source of expiry without widening.

## Deterministic Invariants
Micro-memory `Stored` and `Updated` events must preserve the same deterministic `expires_at` semantics across append and rebuild. Working-memory and long-term-memory rows must continue to rebuild with no expiry. Forgotten rows must remain inactive tombstones with no expiry. Rebuild must remain deterministic from ledger state alone and must not depend on app-layer state, generalized retention workers, or parallel authority paths.

The later implementation run must preserve existing PH1.M meaning: expiry remains derived from the already-published ledger event semantics, not recomputed from a different authority source. No schema change, repo trait change, governance widening, or cross-section contract rename is lawful inside this first canonical seam.

## Out-Of-Scope And Do-Not-Touch Areas
Generalized retention workers are out of scope. Purge orchestration is out of scope. Repo trait changes, schema changes, migration work, and storage-wide refactors are out of scope. Section 05 generalized persistence closure, Section 07 and Section 11 identity work, Section 09 governance closure, Apple client work, and app-layer memory consumers are all out of scope.

This boundary also does not authorize broader PH1.M lifecycle completion claims. If later repo truth still leaves generalized Section 06 closure partial after this slice is implemented, the lawful publication claim is that the first canonical replay-stable expiry seam is published, not that the entire section is complete.

## Ordered Build Plan
1. Start from clean `main` with `HEAD == origin/main` and re-baseline the current replay gap in `crates/selene_storage/src/ph1f.rs`.
2. Preserve the replay gap itself as published repo truth: upstream derivation remains real, append-time preservation remains real, and replay-time loss remains real.
3. Do not freeze or implement a replacement Section 06 slice until a later carrier-selection audit proves one lawful source of replay-time expiry inside the declared boundary or truthfully widens that boundary.
4. Keep any later carrier-selection work out of generalized retention workers, purge orchestration, repo trait changes, schema changes, and cross-section widening unless future repo truth proves that widening is necessary.
5. Publish only this repo-truth correction now, with broader Section 06 still truthfully described as partial and the next implementation winner marked `NOT_EXPLICIT`.

## Verification And Publication Proof
A later implementation run may begin only after repo truth proves one lawful replay carrier for expiry recovery. That later proof must still preserve the current truth that the old replay path passes `None` into `apply_memory_event_to_current` at [ph1f.rs#L3972](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L3972), that the rebuild proof family remains rooted at [ph1f.rs#L24170](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L24170), and that `MemoryLedgerRow` currently lacks `expires_at` at [ph1f.rs#L1022](/Users/selene/Documents/Selene-OS/crates/selene_storage/src/ph1f.rs#L1022).

The current publication proof for this correction run is narrower: the replay gap remains explicitly recorded, the carrier mismatch is now explicitly recorded, the next Section 06 implementation winner is explicitly `NOT_EXPLICIT`, and broader Section 06 closure remains partial. No later implementation claim is lawful until those truths change.

## Stop Conditions
Stop if a later implementation proposal still assumes `MemoryLedgerRow` exposes `expires_at` when current repo truth does not. Stop if a later implementation proposal claims the replay fix is already lawfully bounded to `crates/selene_storage/src/ph1f.rs` without first resolving the carrier mismatch. Stop if a later implementation requires repo trait changes, schema changes, migrations, generalized retention workers, purge orchestration, or broader PH1.M lifecycle closure without new repo truth explicitly freezing that widened boundary.

Stop if baseline repo truth no longer shows the exact replay-stable expiry gap at the declared anchors. Stop if a later proposal would change contract meaning instead of preserving already-published expiry semantics through replay. Stop if a later proposal reopens Section 05 generalized closure, Section 07 or Section 11 identity work, Section 09 governance closure, or app-layer work before one lawful Section 06 carrier is explicit.
