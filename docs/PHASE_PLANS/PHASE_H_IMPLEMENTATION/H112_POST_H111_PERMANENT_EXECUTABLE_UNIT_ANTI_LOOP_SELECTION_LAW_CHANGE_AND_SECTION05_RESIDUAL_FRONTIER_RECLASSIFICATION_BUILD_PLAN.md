# H112 Post-H111 Permanent Executable-Unit Anti-Loop Selection Law Change and Section05 Residual Frontier Reclassification Build Plan

## Objective

H112 is the first post-H111 docs-only permanent executable-unit anti-loop selection-law publication.

H112 does not claim that repo truth alone discovered a unique exact winner.

H111 truth remains preserved exactly.

The recurring failure mode is now explicit: the remaining frontier can be cluster-shaped even when row-level discovery stays `NOT_EXPLICIT`.

H112 permanently changes next-build selection from row-first selection to exact executable-unit selection.

## Exact Executable-Unit Selection Law

The repo now has an exact executable-unit selection law.

An exact executable unit may be either:

- one exact row that passes the uniqueness gate
- one exact coupled cluster of unresolved rows that share the same canonical carrier and proof surface

The uniqueness gate requires:

- one unique primary canonical carrier
- one unique smallest blocker
- one unique proof target
- one unique completion evidence path
- and no preserved exclusion class

Repeated same-pool row-level discovery is prohibited after one narrowed `NOT_EXPLICIT` pass unless repo truth changes.

The next lawful move after such a `NOT_EXPLICIT` pass must instead be:

- executable-unit reclassification
- exact coupled-cluster selection under H104
- or implementation of an already-selected exact executable unit

H112 permanently establishes frontier row classes:

- `SELECTABLE_BUILD_UNIT`
- `COUPLED_CLUSTER_MEMBER`
- `DOC_ONLY`
- `SUMMARY_ONLY`
- `ADJACENT_PROOF_EXCLUDED`
- `BROADER_THAN_SEAM_EXCLUDED`

Excluded classes are not lawful direct implementation winners.

## Current Section 05 Residual Frontier

Section 05 remains the earliest unresolved workstream.

The current post-H110 / post-H111 Section 05 residual executable units are exactly:

- residual executable unit A = `S05-02 + S05-13 + S05-19`
- residual executable unit B = `S05-05 + S05-07`

`S05-01`, `S05-08`, `S05-11`, `S05-12`, and `S05-21` remain excluded exactly as preserved by current repo truth.

`S05-09`, `S05-10`, `S05-15`, and `S05-20` remain exhausted by already-live H110 proof.

## Publication Result

H112 is now published as a docs-only permanent executable-unit anti-loop selection-law change.

Section 11 remains fully complete.

PH1.LAW remains `FULLY_WIRED`.

No executable unit was selected in this run.

This run does not choose residual executable unit A or residual executable unit B.

This run does not authorize implementation.

No implementation occurred in this run.

This run does not implement any source change.

The purpose of H112 is to permanently stop repeated same-pool discovery loops without falsifying post-H111 repo truth.
