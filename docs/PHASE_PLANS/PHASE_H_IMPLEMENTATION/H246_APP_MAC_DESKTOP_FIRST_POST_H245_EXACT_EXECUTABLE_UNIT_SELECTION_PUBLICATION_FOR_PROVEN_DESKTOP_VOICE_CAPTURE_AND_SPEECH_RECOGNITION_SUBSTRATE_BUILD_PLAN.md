# H246 APP_MAC_DESKTOP First Post-H245 Exact Executable-Unit Selection Publication for Proven Desktop Voice Capture and Speech Recognition Substrate Build Plan

## Objective

H246 is the first canonical post-H245 APP_MAC_DESKTOP exact executable-unit selection publication for bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate`.

H246 does not claim that current repo truth alone discovered a unique exact APP_MAC_DESKTOP implementation winner beyond this selected seam.

H245 remains the live bounded native macOS `desktop_permission_posture` implementation publication through master truth.

H243 remains the live broader real-operation foundation selection publication.

H237 remains the live same-frontier docs-only loop-freeze law publication.

H236 remains the live post-H235 APP_MAC_DESKTOP residual frontier `NOT_EXPLICIT` correction.

APP_MAC_DESKTOP remains implementation-partial.

## Basis

H245 remains live through master truth as the first canonical post-H244 selected bounded native macOS `desktop_permission_posture` implementation build.

H244 remains live as the exact executable-unit selection publication for bounded native macOS `desktop_permission_posture`.

H243 remains live as the first intentional broader APP_MAC_DESKTOP real-operation foundation selection.

The current broader APP_MAC_DESKTOP residual selection remains exactly:

- `desktop_permission_posture`
- `desktop_voice_capture_and_speech_recognition_substrate`
- `desktop_canonical_runtime_bridge`
- `desktop_authoritative_reply_rendering`
- `desktop_authoritative_reply_playback`

H237 remains preserved as the permanent same-frontier docs-only publication loop-freeze law.

H236 remains preserved as the first canonical post-H235 APP_MAC_DESKTOP wake-entry and broader-authority residual frontier `NOT_EXPLICIT` correction.

H235 implementation remains preserved in substance.

H234 exact executable-unit selection remains preserved in substance.

H104 intentional-selection authority remains preserved.

H112 executable-unit anti-loop authority remains preserved.

Current native macOS app plist already preserves exact `NSMicrophoneUsageDescription` and exact `NSSpeechRecognitionUsageDescription` in [Info.plist#L25](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/Info.plist#L25).

Current native macOS shell source still lacks bounded explicit voice capture and bounded non-authoritative transcript-preview substrate in [DesktopSessionShellView.swift#L1](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift#L1).

Current iPhone native shell already preserves the bounded explicit voice controller and affordance baseline through exact `VoicePermissionState`, exact `ExplicitVoiceCaptureController`, exact `Start explicit voice turn`, and exact `Stop capture and prepare voice request` in [SessionShellView.swift#L2206](/Users/selene/Documents/Selene-OS/apple/iphone/SeleneIPhone/SessionShellView.swift#L2206) and [SessionShellView.swift#L4695](/Users/selene/Documents/Selene-OS/apple/iphone/SeleneIPhone/SessionShellView.swift#L4695).

Current native macOS shell remains bounded and evidence-first through [DesktopSessionShellView.swift#L2221](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift#L2221) and [DesktopSessionShellView.swift#L2331](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift#L2331).

The smallest coherent exact seam exposed inside the H243-selected broader residual cluster after H245 is therefore bounded native macOS explicit voice capture and non-authoritative transcript-preview substrate through:

- `VoicePermissionState`
- `ExplicitVoiceTurnRequestState`
- `ExplicitVoiceCaptureController`
- `explicitVoiceEntryAffordanceCard`
- `explicitVoiceTranscriptPreviewCard`
- `explicitVoicePendingRequestCard`

This seam is narrower than:

- `desktop_canonical_runtime_bridge`
- `desktop_authoritative_reply_rendering`
- `desktop_authoritative_reply_playback`

Under H104, H112, H237, H243, H244, and H245, intentional exact selection is therefore the lawful next move after the H245 implementation.

## Exact Selection

The exact selected later implementation-first executable unit is now:

- bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate`

The exact selected adjacent internal slice set is now exactly:

- `VoicePermissionState`
- `ExplicitVoiceTurnRequestState`
- `ExplicitVoiceCaptureController`
- `explicitVoiceEntryAffordanceCard`
- `explicitVoiceTranscriptPreviewCard`
- `explicitVoicePendingRequestCard`

The exact selected implementation description is:

- add bounded native macOS explicit voice capture and non-authoritative transcript-preview substrate through exact `VoicePermissionState`, exact `ExplicitVoiceTurnRequestState`, exact `ExplicitVoiceCaptureController`, exact `explicitVoiceEntryAffordanceCard`, exact `explicitVoiceTranscriptPreviewCard`, and exact `explicitVoicePendingRequestCard`

This exact unit is intentionally selected because it is the smallest coherent exact seam inside H243's broader selected cluster after H245 implemented `desktop_permission_posture`.

This run does not implement APP_MAC_DESKTOP.

This run does not authorize implementation in the same run.

No source code changes occur in this run.

No implementation occurs in this run.

## Publication Result

H246 is now published as the first exact executable-unit selection under H245 for bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate`.

H245 truth remains preserved.

H244 truth remains preserved.

H243 truth remains preserved.

H237 truth remains preserved.

H236 truth remains preserved.

APP_MAC_DESKTOP remains partial.

The exact selected later implementation-first executable unit is now bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate`.

The exact selected adjacent internal slice set is now exactly:

- `VoicePermissionState`
- `ExplicitVoiceTurnRequestState`
- `ExplicitVoiceCaptureController`
- `explicitVoiceEntryAffordanceCard`
- `explicitVoiceTranscriptPreviewCard`
- `explicitVoicePendingRequestCard`

Current native macOS shell source still lacks bounded explicit voice capture and bounded non-authoritative transcript-preview substrate.

Current iPhone native shell already preserves the matching bounded explicit voice capture baseline.

No implementation landed in H246.

No post-H246 next exact winner is published in this run.

## Out Of Scope

H246 does not authorize:

- any source-code edit
- any test edit
- any contract change
- any runtime-law change
- any governance change
- any adapter change
- any workflow or tooling change
- any claim of wake parity
- any claim of proven native macOS wake-listener integration
- any claim of autonomous unlock
- any claim that desktop canonical runtime bridge, authoritative reply rendering, or authoritative reply playback are already implemented in the native macOS app
