# H238 APP_IPHONE First Post-H102 Exact Executable-Unit Selection Publication for Proven TTS Resume Snapshot Topic Hint Visible Native-Shell Build Plan

## Objective

H238 is the first canonical post-H102 APP_IPHONE exact executable-unit selection publication for proven separate `tts_resume_snapshot.topic_hint` visible native-shell closure.

H237 remains live as the permanent same-frontier docs-only publication loop freeze selection-law change.

This run is lawful under H237 because same-frontier repo truth changed after H102 on the APP_IPHONE interrupt heavy-detail native-shell seam.

No implementation occurs in this run.

No source code changes in this run.

## Basis

H102 remains live as the first post-H101 canonical APP_IPHONE interrupt resume-buffer heavy-detail hydration frontier `NOT_EXPLICIT` publication correction.

H101 implementation remains live.

H100-H88 implementation truth remains live in substance.

H79-H87 route and shell truth remains live in substance.

Current native iPhone shell source now parses bounded resume-buffer evidence through exact:

- `resume_buffer_answer_id`
- `resume_buffer_spoken_prefix`
- `resume_buffer_unsaid_remainder`
- `tts_resume_snapshot.answer_id`
- `tts_resume_snapshot.spoken_cursor_byte`
- `tts_resume_snapshot.response_text`

Current native iPhone shell source still does not parse or render exact separate `tts_resume_snapshot.topic_hint`.

Current shared PH1.X contract source still preserves exact `tts_resume_snapshot.topic_hint`.

Current repo truth now lawfully exposes one exact remaining direct carrier-backed native-shell implementation unit inside the H102-preserved broader heavy-detail frontier.

APP_IPHONE remains first-class but non-authority, `EXPLICIT_ONLY`, and cloud-authoritative parity only.

## Exact Executable-Unit Selection

The maximum coherent adjacent exact implementation slice set now exposed on this changed frontier is exactly:

- `tts_resume_snapshot.topic_hint`

No larger adjacent exact set remains truthful on this same bounded native-shell seam because the neighboring direct heavy-detail carriers already landed in source truth and are already preserved in current master-doc truth.

The exact remaining direct carrier and exact later implementation-first executable unit is bounded `INTERRUPT_VISIBLE` cloud-authored `tts_resume_snapshot.topic_hint` visible native shell.

The exact selected adjacent internal slice set is exactly:

- `tts_resume_snapshot.topic_hint`

This run does not widen into broader structured TTS-resume-snapshot bundling.

This run does not widen into local topic synthesis, local resume authoring, local dispatch unlock, local authority upgrade, side-button producer, wake parity, or autonomous unlock.

The selected winner in this run is therefore not an implementation slice. It is the H238 docs-only exact executable-unit selection publication.

## Publication Result

H238 is now published as the first canonical post-H102 APP_IPHONE exact executable-unit selection publication for proven separate `tts_resume_snapshot.topic_hint` visible native-shell closure.

H237 truth remains preserved.

H102 truth remains preserved.

H101-H100-H99-H88 truth remain preserved in substance.

`APP_IPHONE-04` remains `PARTIAL`.

`APP_IPHONE-05` remains `PROVEN_COMPLETE`.

`APP_IPHONE-06` remains `PROVEN_COMPLETE`.

No post-H238 next exact winner is published in this run.

## Out Of Scope

H238 does not authorize:

- any source-code edit
- any claim that `tts_resume_snapshot.topic_hint` is already visible in the native iPhone shell
- any claim that broader structured TTS-resume-snapshot hydration is complete
- any claim of wake parity
- any claim of a proven live side-button producer
- any claim of autonomous unlock
- any claim of local topic synthesis
- any claim of local resume authoring
- any claim of local dispatch unlock
- any claim of local authority upgrade
