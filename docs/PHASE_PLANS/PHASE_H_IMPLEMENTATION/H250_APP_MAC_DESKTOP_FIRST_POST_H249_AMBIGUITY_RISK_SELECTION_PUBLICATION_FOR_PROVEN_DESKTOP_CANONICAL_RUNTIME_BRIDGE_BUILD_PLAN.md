# H250 APP_MAC_DESKTOP First Post-H249 Ambiguity-Risk Selection Publication for Proven Desktop Canonical Runtime Bridge Build Plan

## Objective

H250 is the first canonical post-H249 preserved-frontier ambiguity-risk selection publication for bounded native macOS `desktop_canonical_runtime_bridge`.

H250 uses the narrow preserved-frontier ambiguity-risk publication exception preserved by H249.

H250 does not reverse H249, H248, H247, H246, H245, H244, H243, H237, H112, H104, or H236 truth.

No implementation occurs in this run.

No source code changes occur in this run.

APP_MAC_DESKTOP remains implementation-partial.

## Basis

H249 remains live as the permanent lean law-stack alignment and exact-selection compatibility change.

H248 remains live as the permanent lean obvious-slice direct-implementation law change, as interpreted through H249.

H247 remains live as the first canonical post-H246 selected bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate` implementation build.

H246 remains live as the exact executable-unit selection publication for bounded native macOS `desktop_voice_capture_and_speech_recognition_substrate`.

H245 remains live as the first canonical post-H244 selected bounded native macOS `desktop_permission_posture` implementation build.

H244 remains live as the exact executable-unit selection publication for bounded native macOS `desktop_permission_posture`.

H243 remains live as the first broader APP_MAC_DESKTOP real-operation foundation selection.

H237 remains preserved as the same-frontier docs-only loop-freeze law.

H112 remains preserved as executable-unit anti-loop authority.

H104 remains preserved as intentional-selection authority.

H236 remains preserved as the canonical APP_MAC_DESKTOP `NOT_EXPLICIT` correction.

The umbrella desktop program remains preserved.

The current broader APP_MAC_DESKTOP residual selection remains exactly:

- `desktop_permission_posture`
- `desktop_voice_capture_and_speech_recognition_substrate`
- `desktop_canonical_runtime_bridge`
- `desktop_authoritative_reply_rendering`
- `desktop_authoritative_reply_playback`

The current remaining unimplemented broader residual members are exactly:

- `desktop_canonical_runtime_bridge`
- `desktop_authoritative_reply_rendering`
- `desktop_authoritative_reply_playback`

Current native macOS shell source now prepares a bounded pending explicit voice request locally in [DesktopSessionShellView.swift#L997](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift#L997) and renders that bounded pending posture in [DesktopSessionShellView.swift#L4001](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift#L4001).

Current native macOS shell source still does not dispatch that prepared request into the canonical runtime.

Current shared runtime and adapter truth already preserves the canonical desktop carrier family through exact `AppServerIngressRuntime`, exact `AppVoiceIngressRequest`, and exact `AppVoiceTurnExecutionOutcome` in [lib.rs#L118](/Users/selene/Documents/Selene-OS/crates/selene_adapter/src/lib.rs#L118).

Current shared adapter truth already preserves a persistent adapter runtime surface through [lib.rs#L1266](/Users/selene/Documents/Selene-OS/crates/selene_adapter/src/lib.rs#L1266).

Current shared adapter truth already constructs canonical desktop ingress requests through [lib.rs#L4236](/Users/selene/Documents/Selene-OS/crates/selene_adapter/src/lib.rs#L4236).

Current shared adapter truth already proves successful desktop voice-turn runtime execution through [lib.rs#L14496](/Users/selene/Documents/Selene-OS/crates/selene_adapter/src/lib.rs#L14496) and [lib.rs#L15820](/Users/selene/Documents/Selene-OS/crates/selene_adapter/src/lib.rs#L15820).

Current native macOS Xcode target still exposes only the shell app source pair and no native bridge surface, framework linkage, or additional source carrier beyond [project.pbxproj#L10](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop.xcodeproj/project.pbxproj#L10), [project.pbxproj#L23](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop.xcodeproj/project.pbxproj#L23), [project.pbxproj#L42](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop.xcodeproj/project.pbxproj#L42), and [project.pbxproj#L126](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop.xcodeproj/project.pbxproj#L126).

That means current repo truth does not yet expose one already-wired native macOS bridge seam for the next direct implementation build.

The next exact executable unit is therefore genuinely ambiguous or risky under preserved H249 truth because the remaining bridge realization seam is still tied across:

- native macOS bridge-source exposure
- native target membership / linkage exposure
- prepared explicit voice request handoff into the shared canonical runtime carrier family
- bounded canonical runtime outcome carriage without widening into authoritative reply rendering or playback

Under H249's narrow preserved-frontier ambiguity-risk exception, one standalone selection publication is therefore lawful on unchanged same-frontier repo truth to resolve this exact bridge seam.

## Exact Selection

The exact selected later implementation-first executable unit is now:

- bounded native macOS `desktop_canonical_runtime_bridge`

The exact selected adjacent internal slice set is now exactly:

- `SeleneMacDesktopRuntimeBridge.swift`
- `DesktopCanonicalRuntimeBridge`
- `desktopExplicitVoiceIngressRequestBuilder`
- `dispatchPreparedExplicitVoiceRequest`
- `desktopCanonicalRuntimeOutcomeState`

The exact selected implementation description is:

- expose one dedicated native macOS bridge source inside the Xcode target, construct a canonical desktop `AppVoiceIngressRequest` from the already-prepared explicit voice request, dispatch that request into the already-proven shared canonical runtime carrier family, and preserve the bounded canonical runtime outcome in shell-local state without widening into authoritative reply rendering or playback

This exact unit is intentionally selected because it is the smallest coherent exact seam that resolves the current native macOS bridge ambiguity while preserving H249, H237, and H112 truth.

This run does not implement APP_MAC_DESKTOP.

This run does not authorize implementation in the same run.

No implementation occurs in this run.

No source code changes occur in this run.

## Publication Result

H250 is now published as the first canonical post-H249 preserved-frontier ambiguity-risk selection publication for bounded native macOS `desktop_canonical_runtime_bridge`.

H249 truth remains preserved.

H248 truth remains preserved.

H247 truth remains preserved.

H246 truth remains preserved.

H245 truth remains preserved.

H244 truth remains preserved.

H243 truth remains preserved.

H237 truth remains preserved.

H112 truth remains preserved.

H104 truth remains preserved.

H236 truth remains preserved.

APP_MAC_DESKTOP remains partial.

The exact selected later implementation-first executable unit is now bounded native macOS `desktop_canonical_runtime_bridge`.

The exact selected adjacent internal slice set is now exactly:

- `SeleneMacDesktopRuntimeBridge.swift`
- `DesktopCanonicalRuntimeBridge`
- `desktopExplicitVoiceIngressRequestBuilder`
- `dispatchPreparedExplicitVoiceRequest`
- `desktopCanonicalRuntimeOutcomeState`

Current native macOS shell still only prepares bounded explicit voice request posture locally and still does not dispatch into the canonical runtime.

Current shared adapter truth already preserves the canonical runtime carrier family and already proves successful desktop voice-turn execution outside the native macOS Xcode target.

No implementation landed in H250.

No wake parity claim is made.

No proven native macOS wake-listener integration claim is made.

No autonomous unlock claim is made.

No post-H250 next exact winner is published in this run.

## Out Of Scope

H250 does not authorize:

- any source-code edit
- any test edit
- any runtime-law or governance change
- any direct authoritative reply rendering implementation
- any direct authoritative reply playback implementation
- any wake enrollment or wake-listener implementation
- any claim that the native macOS canonical runtime bridge is already implemented
- any claim that authoritative reply rendering is already implemented in the native macOS app
- any claim that authoritative reply playback is already implemented in the native macOS app
