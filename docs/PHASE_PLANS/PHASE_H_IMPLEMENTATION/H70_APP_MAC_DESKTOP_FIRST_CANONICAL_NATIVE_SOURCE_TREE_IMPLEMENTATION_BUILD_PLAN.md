# H70 APP_MAC_DESKTOP: First Canonical Native Source-Tree Implementation Build

## Objective

This is the first canonical APP_MAC_DESKTOP native source-tree implementation build.

H69 implementation remains live and `APP_IPHONE-05` remains `PROVEN_COMPLETE`.

H67 publication remains live and `APP_IPHONE-06` remains `PROVEN_COMPLETE`.

H68 publication remains live and `APP_MAC_DESKTOP-06` remains `PROVEN_COMPLETE`.

H66 publication remains live and the post-H65 Section 11 next exact winner remains `NOT_EXPLICIT`.

Phase F freeze truth remains live.

Phase G freeze truth remains live.

Current strict dependency order still places `APP_IPHONE` before `APP_MAC_DESKTOP`.

APP_MAC_DESKTOP remains first-class but non-authority, wake-word or explicit-entry as designed.

APP_MAC_DESKTOP remains cloud-authoritative and session-bound.

No local authority, no local proof, no local governance, and no local law claim are introduced by this run.

No autonomous unlock claim is lawful in this run.

No proven native macOS wake-listener integration claim is lawful in this run.

This run changes no existing Rust source.

## Current Repo Truth

Current source still preserves `DesktopMicProducer`.

Current source still preserves `VoiceIdentityEmbeddingGateProfiles::mvp_v1_phone_first()` with `desktop_explicit` and `desktop_wake` `optional()`.

`APP_MAC_DESKTOP-06` remains `PROVEN_COMPLETE`.

`APP_IPHONE-04` remains `PARTIAL`.

`APP_IPHONE-05` remains `PROVEN_COMPLETE`.

`APP_IPHONE-06` remains `PROVEN_COMPLETE`.

APP_IPHONE remains earlier in dependency order and is not selected in this run.

## Implemented Result

`APP_MAC_DESKTOP-05` is now `PROVEN_COMPLETE`.

This run creates the first native macOS source tree and local Xcode project in-tree at:

- [project.pbxproj](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop.xcodeproj/project.pbxproj)
- [Info.plist](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/Info.plist)
- [SeleneMacDesktopApp.swift](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/SeleneMacDesktopApp.swift)
- [DesktopSessionShellView.swift](/Users/selene/Documents/Selene-OS/apple/mac_desktop/SeleneMacDesktop/DesktopSessionShellView.swift)

The new native shell is a minimal session-bound placeholder only.

The shell preserves first-class / non-authority / wake-word-or-explicit-entry / cloud-authoritative truth and bounded placeholders for `Session`, `History`, `System Activity`, and `Needs Attention`.

This run does not widen into runtime-law, engine, contract, Section 04-11 source, or APP_IPHONE implementation work.

No post-H70 next exact winner is published in this run.

## Out Of Scope

This H70 implementation does not authorize:

- editing existing Rust source
- networking, persistence, capture production, wake-listener behavior, push-token handling, onboarding mutation, runtime request production, or local authority
- local proof, local governance, or local law behavior
- autonomous unlock claims
- proven native macOS wake-listener integration claims
- APP_IPHONE implementation
- post-H70 target publication
