# Handoff Map (Run 1 Foundation Lock)

Machine-readable source of truth: `HANDOFF_MAP.json`.

Each handoff defines:
- producer_engine
- packet_type
- consumer_engine
- rule
- authority

Hard rule: handoff packet names must exist in `PACKET_SCHEMAS.json`.
