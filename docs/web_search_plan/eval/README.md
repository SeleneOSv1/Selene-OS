# Eval Corpus Notes

- `docs/web_search_plan/replay_corpus.json` is the canonical deterministic replay corpus for CI.
- Additional fixture directories under `docs/web_search_plan/*_fixtures/` that are not currently wired into replay are retained as **future corpus candidates** for expanded deterministic coverage.
