# Realtime Fixtures

`INTERNAL_TEST_ONLY`

These fixtures are deterministic offline test inputs for realtime freshness and stale-refusal checks.
They are consumed by `realtime_tests.rs` and must not be treated as runtime provider payloads.
