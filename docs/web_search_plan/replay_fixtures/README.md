# Replay Fixtures

`INTERNAL_TEST_ONLY`

These fixtures are consumed by the replay harness and continuous evaluation packs.
Some cases are referenced through `fixture_case_id` mappings in `docs/web_search_plan/eval/corpus_packs/*.json` rather than direct filename references.
