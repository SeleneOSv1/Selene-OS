# Builder human-permission gate template.
# Copy to `.dev/builder_permission.env` and fill before running permission checks.

# 0 or 1. If 1 and approval is pending, reminder scheduling evidence is mandatory.
BUSY_MODE=0

# Daily cadence gate (required for both code and launch phases).
# DAILY_REVIEW_DATE_UTC must equal today's UTC date (YYYY-MM-DD).
DAILY_REVIEW_OK=0
DAILY_REVIEW_DATE_UTC=

# Code-change permission gate (pre-code generation)
CODE_APPROVED=0
CODE_APPROVAL_BCAST_ID=
CODE_APPROVAL_DECISION_REF=
CODE_REMINDER_SCHEDULED=0
CODE_REMINDER_REF=

# Launch permission gate (pre-canary / pre-ramp progression)
LAUNCH_APPROVED=0
LAUNCH_APPROVAL_BCAST_ID=
LAUNCH_APPROVAL_DECISION_REF=
LAUNCH_REMINDER_SCHEDULED=0
LAUNCH_REMINDER_REF=
